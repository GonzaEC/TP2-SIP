<configuration>

  <!--
    CONSOLE → JSON line-delimited via LogstashEncoder
    Este appender es el que lee Promtail desde /var/log/pods/...
    Cada línea es un objeto JSON con todos los campos del MDC extraíbles con | json en LogQL.
  -->
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder class="net.logstash.logback.encoder.LogstashEncoder">
      <!-- Renombrar campos para que coincidan con lo que espera el Hit #4 -->
      <fieldNames>
        <timestamp>timestamp</timestamp>
        <version>[ignore]</version>
        <levelValue>[ignore]</levelValue>
        <logger>logger</logger>
        <thread>thread</thread>
        <level>level</level>
        <message>message</message>
        <stackTrace>stack_trace</stackTrace>
      </fieldNames>
      <!-- Incluir los campos MDC como campos JSON de primer nivel -->
      <includeMdcKeyName>producto</includeMdcKeyName>
      <includeMdcKeyName>browser</includeMdcKeyName>
      <includeMdcKeyName>intento</includeMdcKeyName>
    </encoder>
  </appender>

  <!--
    FILE → texto plano rotativo para debugging local.
    Este appender NO va a Loki — solo sirve dentro del pod o montado en un PVC.
  -->
  <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>logs/scraper.log</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
      <fileNamePattern>logs/scraper.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
      <maxFileSize>2MB</maxFileSize>
      <maxHistory>3</maxHistory>
      <totalSizeCap>10MB</totalSizeCap>
    </rollingPolicy>
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%logger{20}] %msg%n</pattern>
    </encoder>
  </appender>

  <root level="INFO">
    <appender-ref ref="CONSOLE"/>
    <appender-ref ref="FILE"/>
  </root>

</configuration>